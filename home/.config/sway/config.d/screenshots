bindsym Print exec /usr/bin/grimshot --notify savecopy active
bindsym $mod+Print exec /usr/bin/grimshot --notify savecopy area
bindsym $mod+Shift+Print exec /usr/bin/grimshot --notify savecopy output

# vi: ft=swayconfig
